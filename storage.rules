rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // BLOCK ALL DIRECT CLIENT ACCESS
    // All uploads and deletions must go through Cloud Functions (backend)
    match /{allPaths=**} {
      allow read: if true; // Keep images publicly readable for UI display
      allow write: if false;
    }
  }
}

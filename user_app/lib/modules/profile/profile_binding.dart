import 'package:get/get.dart';

class ProfileBinding extends Bindings {
  @override
  void dependencies() {
    // Profile is a static display screen — no dedicated controller needed.
    // Add ProfileController here when auth/user data is integrated.
  }
}
